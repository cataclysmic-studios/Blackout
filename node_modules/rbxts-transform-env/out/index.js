"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable @typescript-eslint/no-var-requires */
/* eslint-disable @typescript-eslint/no-non-null-assertion */
/* eslint-disable @typescript-eslint/explicit-module-boundary-types */
var typescript_1 = __importDefault(require("typescript"));
var transformState_1 = require("./class/transformState");
var transformFile_1 = require("./transform/transformFile");
var logProvider_1 = require("./class/logProvider");
var DEFAULTS = {
    verbose: false,
    defaultEnvironment: "production",
    shortCircuitNodeEnv: true,
    expectedVariables: undefined,
};
function transform(program, userConfiguration) {
    var _a;
    userConfiguration = __assign(__assign({}, DEFAULTS), userConfiguration);
    var isProduction = ((_a = process.env.NODE_ENV) !== null && _a !== void 0 ? _a : userConfiguration.defaultEnvironment) === "production";
    if (process.argv.includes("--verbose")) {
        userConfiguration.verbose = true;
    }
    var logger = new logProvider_1.LoggerProvider(userConfiguration.verbose, userConfiguration.verbose);
    if (logger.verbose) {
        logger.write("\n");
    }
    logger.infoIfVerbose("Loaded environment transformer");
    var performedVariableCheck = false;
    return function (context) {
        var e_1, _a;
        var state = new transformState_1.TransformState(program, context, userConfiguration, logger);
        if (state.symbolProvider.moduleFile === undefined) {
            return function (file) { return file; };
        }
        if (userConfiguration.expectedVariables && !performedVariableCheck) {
            var hasDiagnostic = false;
            try {
                for (var _b = __values(Object.entries(userConfiguration.expectedVariables)), _c = _b.next(); !_c.done; _c = _b.next()) {
                    var _d = __read(_c.value, 2), variable = _d[0], variableRequireConfig = _d[1];
                    var hasVariable = state.environmentProvider.has(variable);
                    if (!hasVariable) {
                        if (typeof variableRequireConfig === "string") {
                            if (variableRequireConfig === "error" ||
                                (isProduction && variableRequireConfig === "errorOnProduction")) {
                                logger.error("Expected enviroment variable: '".concat(variable, "'"));
                                hasDiagnostic = true;
                            }
                            else if (variableRequireConfig === "warn") {
                                logger.warnIfVerbose("Missing environment variable '".concat(variable, "'"));
                            }
                        }
                        else {
                            var _e = __read(variableRequireConfig, 2), handler = _e[0], message = _e[1];
                            if (handler === "error" || (isProduction && handler === "errorOnProduction")) {
                                logger.error(message);
                                hasDiagnostic = true;
                            }
                            else if (handler === "warn") {
                                logger.warnIfVerbose(message);
                            }
                        }
                    }
                }
            }
            catch (e_1_1) { e_1 = { error: e_1_1 }; }
            finally {
                try {
                    if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
                }
                finally { if (e_1) throw e_1.error; }
            }
            if (hasDiagnostic) {
                throw new Error("Required environment variable(s) have not been configured - see above");
            }
            performedVariableCheck = true;
        }
        return function (file) {
            var e_2, _a;
            var printFile = false;
            var leading = typescript_1.default.getLeadingCommentRanges(file.getFullText(), 0);
            if (leading) {
                var metaComment = "// @rbxts-transform-env";
                var srcFileText = file.getFullText();
                try {
                    for (var leading_1 = __values(leading), leading_1_1 = leading_1.next(); !leading_1_1.done; leading_1_1 = leading_1.next()) {
                        var leadingComment = leading_1_1.value;
                        var comment = srcFileText.substring(leadingComment.pos, leadingComment.end);
                        if (comment.startsWith(metaComment)) {
                            var metaTags = comment.substring(metaComment.length + 1).split(" ");
                            if (metaTags.includes("debug:print_file")) {
                                printFile = true;
                            }
                            break;
                        }
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (leading_1_1 && !leading_1_1.done && (_a = leading_1.return)) _a.call(leading_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            }
            var result = (0, transformFile_1.transformFile)(state, file);
            if (printFile) {
                var printer = typescript_1.default.createPrinter({});
                console.log(printer.printFile(result));
            }
            return result;
        };
    };
}
exports.default = transform;
