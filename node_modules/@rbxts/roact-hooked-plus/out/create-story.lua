-- Compiled with roblox-ts v2.1.0
local TS = _G[script]
local Roact = TS.import(script, TS.getModule(script, "@rbxts", "roact").src)
local _roact_hooked = TS.import(script, TS.getModule(script, "@rbxts", "roact-hooked").src)
local withHookDetection = _roact_hooked.withHookDetection
local withHooks = _roact_hooked.withHooks
local function createStory(...)
	local args = { ... }
	return function(frame)
		withHookDetection(Roact)
		local handle = Roact.mount(Roact.createElement(withHooks(unpack(args))), frame)
		return function()
			Roact.unmount(handle)
		end
	end
end
return {
	createStory = createStory,
}
